{
  "name": "fraud-detection-platform-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start:all": "node start-all.js",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/public/* public/ 2>/dev/null || true",
    "start": "cd frontend && npm run start"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "clsx": "^2.1.1",
    "lucide-react": "^0.563.0",
    "postcss": "^8.5.6",
    "tailwind-merge": "^3.4.0",
    "tailwindcss": "^4.1.18"
  }
}
